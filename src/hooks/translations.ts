
// Este archivo se mantiene para compatibilidad, pero ahora importa del nuevo sistema modular
import { translations, Language, Translations } from './translations/index';

// Re-exportación para mantener la compatibilidad con el código existente
export { translations };
export type { Language, Translations };
